import { BaseConfigFile } from 'src/models'

export default {
  base_path: './src',
  nextjs: true,
  expect_param: true, // CLI will wait for a param for parsing on the templates
  typescript: true
} as BaseConfigFile
